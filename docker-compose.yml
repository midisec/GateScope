services:
  gate-pattern-scanner:
    build: .
    ports:
      - "8777:8777"
    environment:
      HOST: 0.0.0.0
      PORT: 8777
      NO_BROWSER: 1
    volumes:
      - ./data:/app/data
    restart: unless-stopped
